# Add user installed packages here with: pip3 list --not-required --format=freeze > requirements.txt
# Before this Run: pip install --upgrade pip setuptools wheel
# Install with: pip install -r requirements.txt

setuptools
wheel
numpy                  # sudo apt install libatlas-base-dev
sounddevice            # sudo apt install libportaudio2
pyaudio                # sudo apt install portaudio19-dev
gTTS
openai
python-dotenv
pvporcupine            # sudo apt install libc6 libstdc++6 (usually already present)
Flask
Flask-WTF
email-validator
passlib
markdown
Flask-SocketIO
spotipy
wtforms
requests
websocket-client
rapidfuzz
rasa
spidev
feedparser