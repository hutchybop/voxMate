# Add user installed packages here with: pip3 list --not-required --format=freeze > requirements.txt
# Before this Run: pip install --upgrade pip setuptools wheel
# Install with: pip install -r requirements.txt

setuptools
wheel
numpy
sounddevice
pyaudio # Requires portaudio, on mac: brew install portaudio
gTTS
openai
python-dotenv
huggingface_hub
wavio
vosk
pyttsx3
pvporcupine
flask
pymongo
flask_wtf
email_validator
passlib
markdown
flask_socketio